# Paste from Supabase → Project Settings → API Keys (miltoncwlam@gmail.com).
# Same three names go on Vercel. This file is gitignored.
# Do not set NEXT_PUBLIC_API_BASE to localhost — production must use same-origin /api.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
