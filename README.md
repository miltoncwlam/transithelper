# TransitBuddy V1.0

This branch is the original HTML file pasted into chat on 16 Aug 2026, before the Next.js app existed.

Open `original.html` in a browser. It talks to public KMB / Citybus / MTR APIs from the page (no `/api` server).

Current product is on `main`. Full version list: see `VERSIONS.md` on `main`.
